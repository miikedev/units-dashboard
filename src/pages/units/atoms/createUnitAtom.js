import { atom } from "jotai";

export const stateIdAtom = atom(null);
export const districtIdAtom = atom(null);
export const townshipIdAtom = atom(null);
export const statusAtom = atom(null);
export const positionIdAtom = atom(null);
